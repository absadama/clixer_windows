/**
 * Dashboard Routes
 * Full dashboard data loading with all widgets
 * 
 * NOTE: The handleDashboardFull function remains in index.ts
 * due to complex metric execution dependencies.
 */

import { Router } from 'express';

const router = Router();

// Dashboard endpoints are handled in index.ts due to complex dependencies
// - GET /dashboard/:designId/full
// - POST /dashboard/:designId/full

export default router;
