# ╔═══════════════════════════════════════════════════════════════╗
# ║  CLIXER - PRODUCTION ENVIRONMENT TEMPLATE                     ║
# ║                                                                 ║
# ║  Bu dosyayı .env olarak kopyalayın ve değerleri doldurun:     ║
# ║  cp env.example .env                                           ║
# ╚═══════════════════════════════════════════════════════════════╝

# ============================================
# PostgreSQL (Clixer Uygulama DB)
# ============================================
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_HOST=localhost
DB_PORT=5432
DB_NAME=clixer
DB_USER=clixer
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DATABASE_URL=postgresql://clixer:CHANGE_ME_STRONG_PASSWORD@localhost:5432/clixer

# ============================================
# ClickHouse (Analytics DB)
# ============================================
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_USER=clixer
CLICKHOUSE_PASSWORD=CHANGE_ME_STRONG_PASSWORD
CLICKHOUSE_URL=http://localhost:8123

# ============================================
# Redis (Cache & Pub/Sub)
# ============================================
REDIS_URL=redis://localhost:6379

# ============================================
# JWT (Kimlik Doğrulama)
# ============================================
# ÖNEMLİ: En az 64 karakter, rastgele oluşturulmuş olmalı!
# Oluşturmak için: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_RANDOM_64_CHAR_SECRET

# ============================================
# Node.js Environment
# ============================================
NODE_ENV=production

# ============================================
# CORS (İzin Verilen Originler)
# ============================================
# Virgülle ayırarak birden fazla domain ekleyebilirsiniz
# CORS_ORIGINS=https://analytics.sirketiniz.com,https://app.sirketiniz.com
CORS_ORIGINS=*

# ============================================
# Rate Limiting
# ============================================
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=200

# ============================================
# Servis Portları (Genellikle değiştirmeye gerek yok)
# ============================================
GATEWAY_PORT=4000
AUTH_SERVICE_PORT=4001
CORE_SERVICE_PORT=4002
DATA_SERVICE_PORT=4003
NOTIFICATION_SERVICE_PORT=4004
ANALYTICS_SERVICE_PORT=4005

# ============================================
# E-posta (Bildirimler için - opsiyonel)
# ============================================
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=noreply@example.com
# SMTP_PASS=your_smtp_password
# SMTP_FROM=Clixer Analytics <noreply@example.com>

# ============================================
# Admin Güvenlik (opsiyonel)
# ============================================
# IP Whitelist - sadece bu IP'lerden admin paneline erişim
# Virgülle ayır, * tümüne izin verir
# ADMIN_IP_WHITELIST=192.168.1.0/24,10.0.0.1

# ============================================
# Performans Ayarları (opsiyonel)
# ============================================
# CACHE_ENABLED=true
# CACHE_REDIS_MAX_MEMORY=2gb
# CACHE_DASHBOARD_TTL=900

