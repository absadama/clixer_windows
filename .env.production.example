# ========================================
# CLIXER - PRODUCTION Environment Variables
# ========================================
# DİKKAT: Bu dosyayı .env.production olarak kopyalayın
# TÜM değerleri güvenli rastgele değerlerle DEĞİŞTİRİN!
# Bu değerleri ASLA commit etmeyin!

# ========================================
# JWT AUTHENTICATION (ZORUNLU!)
# ========================================
# Minimum 64 karakter rastgele string kullanın
# Oluşturmak için: openssl rand -base64 48
JWT_SECRET=BURAYA_64_KARAKTER_RASTGELE_STRING_KOYUN_ASLA_BU_DEGERI_KULLANMAYIN!
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d

# ========================================
# ENCRYPTION KEY (ZORUNLU!)
# ========================================
# Veritabanı şifrelerini şifrelemek için
# Oluşturmak için: openssl rand -base64 32
ENCRYPTION_KEY=BURAYA_32_KARAKTER_RASTGELE_STRING_KOYUN!

# ========================================
# DATABASE - PostgreSQL (ZORUNLU!)
# ========================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=clixer
POSTGRES_PASSWORD=GUCLU_RASTGELE_SIFRE_EN_AZ_16_KARAKTER!
POSTGRES_DB=clixer

# ========================================
# DATABASE - ClickHouse (ZORUNLU!)
# ========================================
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_USER=clixer
CLICKHOUSE_PASSWORD=GUCLU_RASTGELE_SIFRE_EN_AZ_16_KARAKTER!
CLICKHOUSE_DATABASE=clixer_analytics

# ========================================
# CACHE - Redis
# ========================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=REDIS_SIFRESI_AYARLAYIN!

# ========================================
# APPLICATION
# ========================================
NODE_ENV=production
LOG_LEVEL=warn

# ========================================
# CORS (ZORUNLU - Production)
# ========================================
# Virgülle ayrılmış izin verilen domain'ler
# ASLA * (wildcard) kullanmayın!
# Örnek: https://app.sirketiniz.com,https://admin.sirketiniz.com
CORS_ORIGIN=https://clixer.sirketiniz.com

# ========================================
# SERVICES PORTS
# ========================================
GATEWAY_PORT=4000
AUTH_SERVICE_PORT=4001
CORE_SERVICE_PORT=4002
DATA_SERVICE_PORT=4003
NOTIFICATION_SERVICE_PORT=4004
ANALYTICS_SERVICE_PORT=4005

# ========================================
# FRONTEND
# ========================================
VITE_API_URL=/api

# ========================================
# SSL/TLS (Önerilen)
# ========================================
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem

# ========================================
# LDAP (Opsiyonel - Kurumsal SSO)
# ========================================
# LDAP_ENABLED=true
# LDAP_URL=ldaps://ldap.sirketiniz.com:636
# LDAP_BIND_DN=cn=clixer,ou=services,dc=sirketiniz,dc=com
# LDAP_BIND_PASSWORD=LDAP_BAGLANTI_SIFRESI
# LDAP_BASE_DN=dc=sirketiniz,dc=com

# ========================================
# GÜVENLİK KONTROL LİSTESİ
# ========================================
# [ ] JWT_SECRET en az 64 karakter mi?
# [ ] POSTGRES_PASSWORD en az 16 karakter mi?
# [ ] CLICKHOUSE_PASSWORD en az 16 karakter mi?
# [ ] CORS_ORIGIN spesifik domain'ler mi? (* değil)
# [ ] NODE_ENV=production ayarlı mı?
# [ ] SSL/TLS sertifikası yapılandırıldı mı?
# [ ] Firewall kuralları ayarlandı mı?
# [ ] Bu dosya .gitignore'da mı?
